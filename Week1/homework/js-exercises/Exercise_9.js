// 1 ) The value of x would be 1 as the % works as remainder operator which returns the remainder 
// of the division between the two variables .

// 2 ) The same as number one .
// 3 ) The same as number one and two.